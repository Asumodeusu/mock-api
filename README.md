# mock-api

To install dependencies:

```bash
bun install
```

To run
чтобы запустить нужно - открыть консоль и ввести:

```bash
bun run start:api
```

This project was created using `bun init` in bun v1.1.17. [Bun](https://bun.sh) is a fast all-in-one JavaScript runtime.
